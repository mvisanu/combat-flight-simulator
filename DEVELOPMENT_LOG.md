# Development log

## 2026-09-05 — Inspection and project bootstrap

- Initial repository: only `prompt.md` and Git metadata. No Unity settings, packages, scenes, models, prefabs, physics, input, camera, terrain, textures, audio, UI or Asset Store content. No baseline game exists to compile.
- Installed editors: 6000.0.79f1 and 6000.5.3f1. Selected installed Unity 6 LTS 6000.0.79f1 without upgrading any existing project.
- Reused the installed official Universal 3D project template, including URP renderer and pipeline assets. Packages include URP 17.0.4, Input System 1.8.1 and Unity Test Framework 1.4.2. Lightweight custom camera; no Cinemachine dependency.
- Started baseline template import/compile before gameplay integration. Original prompt preserved.
- Work ownership: flight/input agent; weapons/damage/effects/audio agent; fighter AI agent; root handles integration, mission, environment, model generation, camera, HUD, editor build and acceptance checks.
- Placeholder geometry and synthesized audio are authorized by the brief. No external art licenses or downloads required.
- Performance targets remain unverified until player profiling; no claims based only on architectural choices.

## 2026-09-05 — First integration and measured acceptance

- Implemented flight/input, combat/damage/effects/audio, deterministic AI, generated aircraft/control surfaces/LODs, procedural Pacific environment/ocean shader, camera modes, HUD, menus/training options, mission definition/outcomes/reset, and floating origin.
- Core compile initially caught Input System 1.8.1's named-argument difference and a missing screen-capture engine module. Both fixed. Earlier initial-import missing-type errors occurred while agent source files were being written; integrated source subsequently compiled cleanly.
- Flight test harness required editor scene creation rather than runtime SceneManager APIs, plus synchronized initial transform/body placement for its induced-stall fixture. Corrected harness without weakening flight assertions.
- `Logs/acceptance-5.log`: 19 flight assertions pass, including neutral 180 s at 112–126.4 m/s, max angular rate 0.005 rad/s, throttle matched trajectories 108.2 vs 128.1 m/s, recovery from 35° to 2.9° AoA, and 120 s constrained-level speeds P51 151.9 vs Zero 122.8 m/s. Constraints occur only inside the comparison fixture.
- Ballistic acceptance passes: six gun emission, delayed hit arrival, convergence, shooter exclusion, actual collider hits, damage degradation/durability, finite ammo/training and one-time destruction events.
- AI approach/perception/terrain checks passed 120 s, but report inspection found no actual firing. Added a stronger firing acceptance requirement and corrected constant pitch bias; tuning continues until real engagement shots occur.
- First Windows development build succeeded: approximately 140 MB, 53 s. First rendered smoke run verified pause, physics, player firing, AI approach, victory and defeat, but screenshot capture failed and consequently the smoke test correctly reported failure. Screenshot capture is being corrected before final verification.
- Initial rendered run on RTX 5080 Laptop GPU / Ryzen 9 9955HX3D averaged 119.7 FPS with 8.34 ms main-thread average at a 120 FPS cap. This is not evidence for midrange hardware. Peak recorded GC/frame was 9,999 B; HUD and setup work still allocate.
- Resource review added explicit destruction of generated island/wing meshes and environment materials on restart. Runtime damage zones now have hittable generated geometry, including pilot, fuel and control surfaces.

## 2026-09-05 — Delivery verification

- `Logs/final-acceptance.log`: all core checks pass. AI acceptance now requires actual firing during a 120-second four-fighter engagement as well as aligned/misaligned firing checks. Two fighters fired; all four acquired and approached the player, with no ocean impacts or nonfinite state. AI fixes addressed pitch trim, local aiming axes, stale aiming points and conflicting squad avoidance. The firing cone remains 1.8°, with an 800 m firing range.
- Final player build: `Builds/Windows/PacificFighterSweep.exe`; Unity 6000.0.79f1, URP, Windows x64 development build. Build log: `Logs/windows-build-delivery.log`. No unresolved compiler or shader errors.
- Runtime tests use real rendered player scenes, physical aircraft, weapons and effects. Both 1 vs 4 and 1 vs 8 pass: scene initialization, URP, pause, finite flight, player ammo use, AI approach and actual gunfire, floating-origin shifts during combat, fresh scene restart, reset ammo, victory and defeat. No runtime errors in the final smoke result files.
- Screenshot failures were caused by hidden Windows game windows; visible-window capture succeeds. Early hidden-window FPS numbers are not treated as rendering benchmarks.
- Final eight-enemy test at 1280×720 using DirectX 11 fallback: 119.9 average FPS at a 120 FPS cap; 8.33 ms average main thread; 0.43 ms reported GPU frame time; 46,368 B peak GC/frame after warmup. AI fired 38 rounds. Four-enemy DirectX 12 run at 1600×900: 119.8 FPS, 8.33 ms main thread, 0.18 ms reported GPU time, 46,368 B peak GC/frame, and 34 enemy rounds fired. These are short 35-second initial-engagement samples on RTX 5080 Laptop GPU / Ryzen 9 9955HX3D, not validation on the requested midrange hardware or long-session profiling. Main-thread time includes frame pacing; recorded GPU timing is driver-reported.
- Runtime evidence: `Builds/Windows/SmokeTest4/results.txt`, `SmokeTest8/results.txt`; each directory contains flight, pause, settings and cockpit screenshots, plus a runtime error file (empty for clean runs).
- Independent visual review: **Pass for first playable prototype**. Ocean moiré removed with filtered/distance-faded wave detail; buttons are matte; assists have explicit ON/OFF states; gunsight line transforms work at 720p; translucent high-RPM propeller disk preserves cockpit aim visibility. All five findings resolved. Aircraft/cloud geometry and audio remain intentionally provisional.
- Launch and rebuild instructions are in `README.md` and `Tools/Build.ps1`. All advanced/future features and unverified hardware requirements remain explicitly listed in `TODO.md`; the full long-term specification is not claimed complete.

## 2026-09-05 — Continued development: endurance, sustained AI and preferences

- Delegated sustained AI to the AI agent, fuel/damage/audio integration to the combat agent, and persistent preferences to the flight agent. Root integrated fuel telemetry, expanded live smoke checks, actual keyboard input tests, frame-time/memory sampling and repeatable build/test tools.
- `AircraftFuel` models litres, throttle-scaled litres/hour consumption, damage-dependent litres/second leakage and additional fire drain. `AircraftEngine` owns fuel stepping and stops thrust/power at starvation; `AircraftDamage` extinguishes fuel-fed fire when empty without repairing damage. Engine combustion audio stops while wind remains. Existing aircraft assets explicitly receive their new fuel tuning fields, including separate Zero values.
- New HUD fuel percentage and low/empty/leaking warnings. Mission briefing now reads aircraft/count/altitude/speed/range from the actual mission instead of fixed text.
- Preferences persist graphics preset, master volume, difficulty, lead assist, unlimited ammo and HUD visibility using versioned JSON with clamping/default recovery. Runtime URP clone avoids modifying authored assets. Smoke runs disable preference writes and use deterministic default assists. Developer telemetry is deliberately not saved.
- Sustained AI uses observed target turn rate for bounded curved tactical interception, a relative closing-speed estimate, improved post-pass turns and rotating squad attack priority. No aircraft speed/thrust bonuses or widened firing cone. The 300-second maneuvering fixture passed with three firing participants, two later passes, twelve target circuit legs and minimum AI altitude 3022.6 m. The original straight-flight engagement remains covered. These are isolated AI/flight tests; rendered mission tests separately exercise actual weapon damage.
- Input event tests initially exposed the editor's deliberate suppression of player-action updates. The harness now uses cloned Input System settings and its package-provided edit-mode player-update test flag, restoring settings/devices/actions afterward. Tests do not overwrite user bindings.
- `Logs/continued-verified-build.log`: all flight, combat, fuel, 13 settings checks, 12 keyboard/remapping checks and AI tests pass; Windows build succeeds. Build command now validates and builds in one editor invocation unless explicitly skipped.
- `Tools/SmokeTest.ps1` adds bounded process timeout, fresh-report and exit-code verification. Runtime reports now include p95/p99 frame times, memory samples and actual observed duration. Rendered verification of the continued build is tracked below.
- Continued Windows build passes both rendered 60-second runs: four enemies at 1600 x 900 / DirectX 12 and eight enemies at 1280 x 720 / DirectX 11. Each passes live firing, finite physics, fuel consumption, saved-settings isolation, pause, floating origin, restart/refuelling, damaged-tank starvation, victory and defeat. Both report six enemy rounds fired and closest approach of 38 m. Runtime error files are empty.
- Four-enemy sample: 119.8 average FPS, p95 8.34 ms / p99 8.43 ms, allocated memory 127.6 to 119.3 MiB, peak post-warmup GC/frame 49,770 B. Eight-enemy sample: 119.9 average FPS, p95 8.34 ms / p99 8.44 ms, allocated memory 123.7 to 117.9 MiB, peak GC/frame 51,264 B. Both observed the full requested 60 seconds on RTX 5080 Laptop GPU / Ryzen 9 9955HX3D at the 120 FPS cap. These are short hardware-specific samples, not long-session or midrange-hardware certification; allocations remain a documented optimization task.
- Independent narrow visual review passes at both resolutions: fuel/engine telemetry, empty-fuel warning and selected graphics/ON-OFF settings remain readable and unclipped. No blockers in this review scope. Test processes exited cleanly.
